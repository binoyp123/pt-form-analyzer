# Backend environment (optional — defaults work for local/demo)

# Comma-separated frontend origins for production CORS.
# Leave unset or "*" for local development.
CORS_ORIGINS=*

MAX_UPLOAD_MB=35
MAX_DURATION_SEC=45
